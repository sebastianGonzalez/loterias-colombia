# Entorno virtual (no se sube; se recrea con requirements.txt)
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Logs temporales
server.log
*.log

# La base de datos ahora vive en Postgres (persistente) en producción.
# El SQLite local es solo para desarrollo/tests y NO se versiona.
data/lottery.db
